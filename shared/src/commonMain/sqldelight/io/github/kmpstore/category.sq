CREATE TABLE IF NOT EXISTS category (
    id TEXT NOT NULL PRIMARY KEY, -- Mapping UUID to TEXT
    name TEXT NOT NULL,
    slug TEXT NOT NULL,
    parent_id TEXT
);

insertCategory:
INSERT OR REPLACE INTO category(id, name, slug, parent_id)
VALUES (?, ?, ?, ?);

selectAllCategories:
SELECT * FROM category;