CREATE TABLE IF NOT EXISTS category (
    id TEXT NOT NULL PRIMARY KEY, -- Mapping UUID to TEXT
    name TEXT NOT NULL,
    slug TEXT NOT NULL,
    description TEXT
);

insertCategory:
INSERT OR REPLACE INTO category(id, name, slug, description)
VALUES (?, ?, ?, ?);

selectAllCategories:
SELECT * FROM category;